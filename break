for n in range(100 // 10):
    for k in range(100 // 5):
        for m in range(int(100 / 0.5)):
            if 10*n + 5*k + 0.5*m == 100 and n+k+m == 100:
                print(f'n={n}, k={k}, m={m}')
